//
//  ContentView.swift
//  radial-menu
//
//  Created by Steven Greenberg on 11/22/25.
//

import SwiftUI

/// Placeholder view retained to satisfy template references; not used by the app runtime.
struct ContentView: View {
    var body: some View {
        Text("Radial Menu")
            .padding()
    }
}

// Preview removed in CLI build to avoid macro plugin dependency.
